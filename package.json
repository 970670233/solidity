{
  "devDependencies": {
    "eslint": "^5.16.0",
    "sass": "^1.53.0"
  },
  "dependencies": {
    "ethers": "^5.6.9",
    "fs-extra": "^10.1.0",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_models/ --base-path .o. SimpleStorage.sol"
  }
}
